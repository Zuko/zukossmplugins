//----------------------------------------COMPLETE ACTIONS--------------------------------------------
OnAchievementComplete(argClient, argAction) {
	// ACTIONS
	if (argAction == 1) FakeClientCommand(argClient, "taunt");
}